# Dotfiles of mfillmer

## Getting Started

1. Install ansible e.g. `sudo dnf install ansible`
2. clone this repository and open it in a terminal
3. run `ansible-playbook --ask-become-pass bootstrap.yml`


